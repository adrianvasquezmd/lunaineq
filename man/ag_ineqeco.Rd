% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ag_ineqeco.R
\name{ag_ineqeco}
\alias{ag_ineqeco}
\title{Absolute Ecological Inequality Gap for Aggregated Data}
\usage{
ag_ineqeco(
  data,
  health_indicator_type = c("rate", "ratio", "proportion", "percentage"),
  health_indicator_var = NULL,
  population_weights_var = NULL,
  health_numerator_var = NULL,
  health_denominator_var = NULL,
  health_se_var = NULL,
  analysis_unit_var = NULL,
  equity_stratifier_var = NULL,
  higher_ineq_is_favorable = FALSE,
  rate_scaling_factor = NULL,
  grouping_approach = c("territorial", "weighted_cut", "weighted_midpoint", "fractional"),
  territorial_method = c("quantile", "kmeans", "fisher", "manual", "equal"),
  manual_breaks = NULL,
  n_groups = 5,
  ci_method = c("delta", "bootstrap", "jackknife"),
  n_boot = 2000,
  conf_level = 0.95,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing the grouped ecological data.}

\item{health_indicator_type}{Character. The scale of the health indicator: \code{"rate"}, \code{"ratio"}, \code{"proportion"}, or \code{"percentage"}.}

\item{health_indicator_var}{Character. The name of the column containing the point estimates of the health indicator.}

\item{population_weights_var}{Character. The name of the column containing the population sizes or analytic weights for each ecological unit.}

\item{health_numerator_var}{Character. The name of the column containing the event counts (numerator). Required if raw counts are used.}

\item{health_denominator_var}{Character. The name of the column containing the population sizes (denominator). Required if raw counts are used.}

\item{health_se_var}{Character. The name of the column containing standard errors for the health indicator. Used for delta-method analytical variance.}

\item{analysis_unit_var}{Character. The name of the column identifying each ecological unit (e.g., municipality, province).}

\item{equity_stratifier_var}{Character. The name of the column containing the socioeconomic stratifier (e.g., income, education). Must be numeric or ordered factor.}

\item{higher_ineq_is_favorable}{Logical. If \code{TRUE}, implies that a higher indicator value is favorable. Defaults to \code{FALSE} (e.g., mortality rates).}

\item{rate_scaling_factor}{Numeric. A scaling factor to adjust rates (e.g., \code{1000} for per 1,000 population). If \code{NULL}, it defaults based on \code{health_indicator_type}.}

\item{grouping_approach}{Character. Method to group the ecological units: \code{"territorial"}, \code{"weighted_cut"}, \code{"weighted_midpoint"}, or \code{"fractional"}.}

\item{territorial_method}{Character. If \code{grouping_approach} is \code{"territorial"}, the algorithm to build quantiles: \code{"quantile"}, \code{"kmeans"}, \code{"fisher"}, \code{"manual"}, or \code{"equal"}.}

\item{manual_breaks}{Numeric vector. If \code{territorial_method} is \code{"manual"}, the precise cut-points for grouping.}

\item{n_groups}{Integer. The number of quantiles to form (e.g., 5 for quintiles). Defaults to \code{5}.}

\item{ci_method}{Character. The method for confidence interval estimation: \code{"delta"}, \code{"bootstrap"}, or \code{"jackknife"}.}

\item{n_boot}{Integer. The number of bootstrap replicates if \code{ci_method = "bootstrap"}. Defaults to \code{2000}.}

\item{conf_level}{Numeric. The confidence level for the intervals. Defaults to \code{0.95}.}

\item{seed}{Integer. An optional seed for reproducible resampling.}

\item{verbose}{Logical. Whether to print methodological notes and warnings to the console. Defaults to \code{TRUE}.}
}
\value{
An S3 object of class \code{c("ag_ineqeco", "ag_rg_ineqeco", "list")} containing:
\item{point_estimate}{The point estimate of the absolute gap.}
\item{se}{The standard error of the estimate.}
\item{ci_lower}{The lower bound of the confidence interval.}
\item{ci_upper}{The upper bound of the confidence interval.}
\item{methodological_notes}{Detailed explanations of the analytical approach and variance estimator used.}
\item{results_groups}{A data frame summarizing the grouped data, including group-specific estimates and variance.}
}
\description{
\code{ag_ineqeco()} computes the Absolute Gap (AG) in a health indicator between the most
disadvantaged and most advantaged ecological groups. This is an unweighted ecological
metric that quantifies the absolute scale of inequality across population strata.

The estimand is formally defined as:

\deqn{AG = Y_D - Y_A}

where \eqn{Y_D} denotes the estimate of the health indicator in the most
socioeconomically disadvantaged group, and \eqn{Y_A} denotes the estimate in
the most advantaged group. If higher inequality is considered favorable
(e.g., vaccine coverage), the contrast may be analytically reversed.
}
\examples{
\dontrun{
# Use the built-in paho_data
data(paho_data)
df_2020 <- subset(paho_data, year == 2020)

# Calculate absolute gap
ag <- ag_ineqeco(
  data = df_2020,
  health_indicator_type = "rate",
  health_numerator_var = "maternal_death",
  health_denominator_var = "live_births",
  rate_scaling_factor = 100000,
  analysis_unit_var = "state",
  equity_stratifier_var = "ubn",
  grouping_approach = "fractional",
  n_groups = 5,
  ci_method = "delta"
)
print(ag)
}
}
