% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sii_ineqeco.R
\name{sii_ineqeco}
\alias{sii_ineqeco}
\title{Slope Index of Inequality for Ecological Data}
\usage{
sii_ineqeco(
  data,
  health_indicator_type = c("proportion", "percentage", "rate", "ratio"),
  health_indicator_var = NULL,
  population_weights_var = NULL,
  health_numerator_var = NULL,
  health_denominator_var = NULL,
  equity_stratifier_var,
  analysis_unit_var,
  higher_ineq_is_favorable,
  rate_scaling_factor = NULL,
  models = NULL,
  model_selection_metric = c("MSE", "MAE", "RMSE", "MAPE", "AIC", "BIC"),
  social_position_method = c("classic", "bounded"),
  ci_method = c("wald", "bootstrap", "jackknife"),
  n_boot = 2000,
  conf_level = 0.95,
  seed = NULL,
  verbose = TRUE,
  vcov_type = "HC1",
  ...
)
}
\arguments{
\item{data}{A data frame with one row per ecological unit.}

\item{health_indicator_type}{Indicator type: \code{"proportion"}, \code{"percentage"},
\code{"rate"}, or \code{"ratio"}.}

\item{health_indicator_var}{Optional column name with a precomputed indicator.}

\item{population_weights_var}{Optional column name with population/exposure
weights when using a precomputed indicator.}

\item{health_numerator_var}{Optional column name with numerator counts.}

\item{health_denominator_var}{Optional column name with denominator/exposure
counts.}

\item{equity_stratifier_var}{Column name of the equity stratifier used to
order ecological units.}

\item{analysis_unit_var}{Column name uniquely identifying ecological units.}

\item{higher_ineq_is_favorable}{Logical. \code{TRUE} if higher values of the equity
stratifier indicate greater advantage; \code{FALSE} otherwise.}

\item{rate_scaling_factor}{Scaling factor for rates or ratios from counts.}

\item{models}{Optional character vector of candidate models. If \code{NULL}, all
allowed models for the indicator type and input source are considered.}

\item{model_selection_metric}{Metric used to select the model.}

\item{social_position_method}{\code{"classic"} evaluates extremes at 0 and 1;
\code{"bounded"} evaluates at the observed minimum and maximum social position.}

\item{ci_method}{Confidence interval method: \code{"wald"}, \code{"bootstrap"}, or
\code{"jackknife"}.}

\item{n_boot}{Number of bootstrap replicates when \code{ci_method = "bootstrap"}.}

\item{conf_level}{Confidence level.}

\item{seed}{Optional random seed for resampling.}

\item{verbose}{Logical; passed to internal validation metadata.}

\item{vcov_type}{Robust covariance type passed to \code{sandwich::vcovHC()} when
available for Wald intervals.}

\item{...}{Reserved. Unsupported arguments are rejected with an actionable
message.}
}
\value{
An auditable list with overall estimates, model-selection results,
unit-level predictions, ridit construction, optional resampling replicates,
methodology metadata, and diagnostics.
}
\description{
Estimates the slope index of inequality for ecological data from ecological data using a
population-weighted ridit/social-position variable and model-based prediction
at the disadvantaged and advantaged social-position extremes.
}
\details{
The estimand is \code{SII = Y_advantaged - Y_disadvantaged}.

The function is intentionally a user-facing orchestrator. Shared internal
helpers with the \verb{.sii_rii_*} prefix validate inputs, prepare data, fit and
select models, resample when requested, and format auditable outputs.
}
\examples{
\dontrun{
# Use the built-in paho_data
data(paho_data)
df_2020 <- subset(paho_data, year == 2020)

# Calculate Slope Index of Inequality
sii <- sii_ineqeco(
  data = df_2020,
  health_indicator_type = "rate",
  health_numerator_var = "maternal_death",
  health_denominator_var = "live_births",
  rate_scaling_factor = 100000,
  analysis_unit_var = "state",
  equity_stratifier_var = "ubn",
  higher_ineq_is_favorable = FALSE,
  ci_method = "wald"
)
print(sii)
}
}
