% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci_ineqeco.R
\name{aci_ineqeco}
\alias{aci_ineqeco}
\title{Absolute concentration index for ecological data}
\usage{
aci_ineqeco(
  data,
  health_indicator_type = c("proportion", "percentage", "rate", "ratio"),
  health_indicator_var = NULL,
  health_numerator_var = NULL,
  health_denominator_var = NULL,
  health_se_var = NULL,
  rate_scaling_factor = NULL,
  population_weights_var = NULL,
  equity_stratifier_var,
  analysis_unit_var = NULL,
  higher_ineq_is_favorable = TRUE,
  health_lower_bound = NULL,
  health_upper_bound = NULL,
  bounded_corrections = "none",
  ci_method = c("analytic", "bootstrap", "jackknife"),
  n_boot = 1000L,
  conf_level = 0.95,
  add_smoothed_curves = TRUE,
  smoothing_methods = c("ml", "loess"),
  loess_span = 0.75,
  seed = NULL,
  include_replicates = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame containing ecological-unit data.}

\item{health_indicator_type}{Character. One of \code{"proportion"},
\code{"percentage"}, \code{"rate"}, or \code{"ratio"}.}

\item{health_indicator_var}{Character or NULL. Name of the health indicator
variable if the indicator is supplied directly.}

\item{health_numerator_var}{Character or NULL. Name of the health numerator
variable if the health indicator should be constructed from counts.}

\item{health_denominator_var}{Character or NULL. Name of the health
denominator variable if the health indicator should be constructed from
counts.}

\item{health_se_var}{Character or NULL. Name of the standard error variable
for the ecological-unit/group health indicator. When supplied and
\code{ci_method = "analytic"}, it is used as group-level uncertainty in the
grouped analytic variance approximation.}

\item{rate_scaling_factor}{Numeric or NULL. Scaling factor applied when
\code{health_indicator_type} is \code{"rate"} or \code{"ratio"} and the indicator is
constructed from numerator/denominator data. For example, use 100000 for
maternal mortality ratio per 100,000 live births. Directly supplied rate
indicators are assumed to already be on the desired scale.}

\item{population_weights_var}{Character or NULL. Name of the population or
exposure weight variable. Required when \code{health_indicator_var} is supplied
directly. Optional when numerator/denominator variables are supplied; if
omitted, the denominator is used as the population/exposure weight.}

\item{equity_stratifier_var}{Character. Name of the socioeconomic/equity
stratifier variable.}

\item{analysis_unit_var}{Character or NULL. Name of the ecological-unit
identifier variable.}

\item{higher_ineq_is_favorable}{Logical. If TRUE, higher values of the equity
stratifier represent greater advantage. If FALSE, higher values represent
greater disadvantage and are internally reversed for ordering.}

\item{health_lower_bound}{Numeric or NULL. Lower bound of the health indicator.
Included for API compatibility with \code{rci_ineqeco}, but ignored for ACI.}

\item{health_upper_bound}{Numeric or NULL. Upper bound of the health indicator.
Included for API compatibility with \code{rci_ineqeco}, but ignored for ACI.}

\item{bounded_corrections}{Character. Included for API compatibility with
\code{rci_ineqeco}, but ignored for ACI (always "none").}

\item{ci_method}{Character. One of \code{"analytic"}, \code{"bootstrap"}, or
\code{"jackknife"}.}

\item{n_boot}{Integer. Number of bootstrap replicates when
\code{ci_method = "bootstrap"}.}

\item{conf_level}{Numeric. Confidence level. Default is 0.95.}

\item{add_smoothed_curves}{Logical. Whether to add smoothed concentration
curves to \code{results_curve}.}

\item{smoothing_methods}{Character vector. Any combination of \code{"ml"} and
\code{"loess"}. Used only when \code{add_smoothed_curves = TRUE}.}

\item{loess_span}{Numeric. Span parameter used for LOESS smoothing.}

\item{seed}{Integer or NULL. Optional random seed for bootstrap
reproducibility.}

\item{include_replicates}{Logical. Whether to include bootstrap/jackknife
replicate estimates in the final object.}

\item{verbose}{Logical. Whether to print progress messages.}
}
\value{
An object of class \code{"aci_ineqeco_result"} and
\code{"aci_rci_ineqeco_result"}, containing:
\describe{
\item{summary_stats}{Scalar summary statistics and the primary ACI result.}
\item{results_overall}{Metric estimates, standard errors, and confidence
intervals.}
\item{results_units}{Prepared ecological-unit analytic data.}
\item{results_curve}{Empirical and optional smoothed concentration-curve
data.}
\item{results_replicates}{Bootstrap/jackknife replicate estimates, or
NULL.}
\item{methodology}{Methodological metadata.}
\item{diagnostics}{Nested diagnostics from each workflow step.}
\item{warnings}{Unique warnings generated during the workflow.}
\item{call}{Original matched call.}
}
}
\description{
Estimates the Absolute Concentration Index (ACI) using grouped ecological
data. Each row of \code{data} is treated as an ecological unit or group, such as
a department, region, district, province, country, quintile, or other
aggregated unit.

The function first estimates the empirical grouped Relative Concentration
Index (RCI):

\deqn{C = (2 / \mu) \sum_i f_i y_i R_i - 1}

and then estimates the ACI as:

\deqn{ACI = \mu C}

where \eqn{\mu} is the weighted mean of the health indicator.

The base RCI is also returned in \code{results_overall} for transparency.

Smoothed concentration curves, if requested, are computed for visualization
only and are not used to estimate the RCI, ACI, standard errors, or
confidence intervals.
}
\examples{
\dontrun{
# Use the built-in paho_data
data(paho_data)
df_2020 <- subset(paho_data, year == 2020)

# Calculate Absolute Concentration Index using analytical variance
aci <- aci_ineqeco(
  data = df_2020,
  health_indicator_type = "rate",
  health_numerator_var = "maternal_death",
  health_denominator_var = "live_births",
  rate_scaling_factor = 100000,
  analysis_unit_var = "state",
  equity_stratifier_var = "ubn",
  ci_method = "analytic"
)
print(aci)
}
}
